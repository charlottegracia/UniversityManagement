import { Course } from "../../models/course.js";
import { Department } from "../../models/department.js";
import { Enrollment } from "../../models/enrollment.js";
import { Faculty } from "../../models/faculty.js";
import { Major } from "../../models/major.js";
import { Student } from "../../models/student.js"

function testMe() {
    // // let faculty = new Faculty(1, "lol", "kongen");
    // // let department = new Department(1, "mere lol", faculty);
    // // let major = new Major(1, "lol", department);
    // // let course = new Course(1, "nej", department);
    // // let student = new Student(1, "sjops", Date.now(), major);
    // // let enrollment = new Enrollment(4, student, course, Date.now(), 'C');
}