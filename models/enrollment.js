class Enrollment {
    constructor(id, course, enrollmentDate, grade) {
        this.id = id;
        this.course = course;
        this.enrollmentDate = enrollmentDate;
        this.grade = grade;
    }
}

module.exports = Enrollment;